{
  "name": "fanstock-contracts",
  "version": "1.0.0",
  "description": "Fanvest Smart Contracts for democratizing fan tokens",
  "main": "hardhat.config.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:chiliz": "hardhat run scripts/deploy.js --network chilizSpicy",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "dotenv": "^17.2.0"
  }
}
