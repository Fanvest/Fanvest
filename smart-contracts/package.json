{
  "name": "fanvest-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts template for fan tokens on Chiliz",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:spicy": "hardhat run scripts/deploy.js --network spicy",
    "deploy:spicy2": "hardhat run scripts/deploy.js --network spicy2",
    "deploy:simple": "hardhat run scripts/deploy-simple.js --network spicy",
    "setup": "hardhat run scripts/setup-spicy.js --network spicy2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.0.0"
  }
}
